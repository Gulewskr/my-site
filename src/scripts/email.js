const sendToMe = (n, c, t, cb) => {
  /*var form = {
      name: n,
      contact: c,
      message: t,
   }*/

  console.log("Emailing temporarily removed");
  cb(false);
};

export default sendToMe;
